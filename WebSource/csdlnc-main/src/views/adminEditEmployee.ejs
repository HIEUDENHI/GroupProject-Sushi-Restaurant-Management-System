<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chỉnh sửa nhân viên</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <h1>Chỉnh sửa thông tin nhân viên</h1>
    
    <!-- Hiển thị thông báo lỗi hoặc thành công -->
    <% if (errorMessage) { %>
        <p style="color: red; text-align: center;"><%= errorMessage %></p>
    <% } %>
    <% if (successMessage) { %>
        <p style="color: green; text-align: center;"><%= successMessage %></p>
    <% } %>

    <!-- Form chỉnh sửa nhân viên -->
    <form action="/admin/employees/edit-employee/<%= employee.MaNhanVien %>" method="POST" class="form-container">
        <div class="form-group">
            <div>
                <label for="HoTen">Họ tên:</label>
                <input type="text" id="HoTen" name="HoTen" value="<%= employee.HoTen %>" required>
            </div>
            <div>
                <label for="NgaySinh">Ngày sinh:</label>
                <input type="date" id="NgaySinh" name="NgaySinh" value="<%= employee.NgaySinh.toISOString().split('T')[0] %>" required>
            </div>
            <div>
                <label for="GioiTinh">Giới tính:</label>
                <select id="GioiTinh" name="GioiTinh" required>
                    <option value="Nam" <%= employee.GioiTinh === 'Nam' ? 'selected' : '' %>>Nam</option>
                    <option value="Nữ" <%= employee.GioiTinh === 'Nữ' ? 'selected' : '' %>>Nữ</option>
                </select>
            </div>
            <div>
                <label for="MaBoPhan">Mã bộ phận:</label>
                <input type="number" id="MaBoPhan" name="MaBoPhan" value="<%= employee.MaBoPhan %>" required>
            </div>
            <div>
                <label for="MaChiNhanh">Mã chi nhánh:</label>
                <input type="number" id="MaChiNhanh" name="MaChiNhanh" value="<%= employee.MaChiNhanh %>" required>
            </div>
        </div>
        <button type="submit">Cập nhật</button>
    </form>

    <div class="back-button">
        <a href="/admin">
            <button>Quay lại Trang Quản Lý</button>
        </a>
    </div>
</body>
</html>
